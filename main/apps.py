from time import sleep

from django.apps import AppConfig


# class MainConfig(AppConfig):
#     default_auto_field = 'django.db.models.BigAutoField'
#     name = 'main'

    # def ready(self):
    #     from main.management.commands.runapscheduler import sch_mail
    #     sleep(2)
    #     sch_mail()

